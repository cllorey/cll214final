---
title: "Recreating Figure 3 of Schaefer et al. (2000)"
author: "Courtney Lorey"
date: "2026-08-27"
format: html
---

# Background
[insert background]

# Data
The raw data has been processed and cleaned via the 1_clean_data.R script. Load the cleaned data and plot it using the code below. 

```{r}
library(tidyverse)
fig3 <- read_csv("/Users/courtneylorey/Documents/eds214/cll214final/output/clean_data.csv")

ggplot(
    data = fig3,
    mapping = aes(
        x = window_start,
        y = Concentration,
        linetype = Site
    )
) + 
  geom_line() +
  geom_vline(xintercept = ymd("1989-09-18"), linetype = "dashed", linewidth = 0.4) +
  theme_bw() +
  labs(
    title = "Hurricane effects on stream chemistry", y = NULL) +
  theme(
    panel.grid.major = element_blank(),
    panel.grid.minor = element_blank()
  ) +
  theme(legend.title = element_blank()) +
  theme(
    legend.justification = c("right", "top")) +
  facet_grid(vars(Nutrient), scales = "free_y", switch = "y") + 
  theme(strip.placement = "outside") +
  scale_x_date(name = "Years", sec.axis = dup_axis())
```

# Methods
We used a nine week moving average to ...

# Results
[Insert figure]